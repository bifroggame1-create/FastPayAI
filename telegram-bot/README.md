# Telegram Bot для Tech Shop

## Быстрый старт

### 1. Установите зависимости

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Настройте переменные окружения

```bash
cp .env.example .env
```

Отредактируйте `.env`:
- `BOT_TOKEN` - токен от @BotFather
- `WEB_APP_URL` - URL вашего Web App (ngrok или production)

### 3. Запустите бота

```bash
chmod +x start.sh
./start.sh
```

Или напрямую:

```bash
source venv/bin/activate
python bot.py
```

## Команды бота

- `/start` - Приветствие и открытие магазина
- `/help` - Справка по использованию
- `/shop` - Быстрый доступ к магазину

## Кнопка меню

Бот автоматически устанавливает кнопку меню "🛍 Магазин" при запуске.

## Требования

- Python 3.8+
- aiogram 3.13.1
- Запущенные Frontend и Backend серверы
